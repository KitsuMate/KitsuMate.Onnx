# KitsuMate Unity AI Inference Embeddings

Dedicated text-embedding model-set and runtime for `com.unity.ai.inference`. It uses imported `UnityAiInferenceModelAsset` objects and `Int32` token tensors, rather than reusing the ONNX Runtime implementation's `Int64` contract.
